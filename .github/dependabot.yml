# Keeps the SHA-pinned GitHub Actions current: Dependabot bumps the pin and
# updates the trailing `# vX.Y.Z` comment when a new release lands, so pinning
# to an immutable commit doesn't mean going stale.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Batch action bumps into one reviewable PR rather than one per action.
    groups:
      github-actions:
        patterns: ["*"]
