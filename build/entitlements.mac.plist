<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Auto-paste drives Cmd+V through System Events via osascript. In a
         signed build the hardened runtime requires the app (as the responsible
         process) to be entitled to send Apple Events, or macOS refuses with
         -1743. Release builds are currently unsigned, so entitlements are not
         applied to them at all; this is here so signing later does not break
         auto-paste. What releases rely on is NSAppleEventsUsageDescription in
         electron-builder.yml. -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
